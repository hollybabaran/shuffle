package Shuffle.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class Rank_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<Rank> constants = ListSequence.fromList(Rank.getConstants()).iterator();
    while (constants.hasNext()) {
      Rank constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }

  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<Rank> constants = ListSequence.fromList(Rank.getConstants()).iterator();
    while (constants.hasNext()) {
      Rank constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }

  public String fromInternalValue(String value) {
    Rank constant = Rank.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
