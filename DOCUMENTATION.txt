June 12th, 2014

Added CommandBlock which is the parent of SetupBlock and WinConditionBlock. 
SetupBlock looks like this:
    
        setup board :
            <commands...>
        end

Added ConditionBlock which is a Block with one condition
WinConditionBlock looks like this:

        win condition :
            <logical expression here>
        end
